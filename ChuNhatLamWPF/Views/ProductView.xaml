<Window x:Class="ChuNhatLamWPF.Views.ProductView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChuNhatLamWPF.Views"
        xmlns:vm="clr-namespace:ChuNhatLamWPF.ViewModels"
        mc:Ignorable="d"
        Title="Quản lí sản phẩm" Height="450" Width="800">
    <Window.DataContext>
        <vm:ProductViewModel />
    </Window.DataContext>
    <DockPanel Margin="10">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,10">
            <Button Content="Thêm" Command="{Binding AddCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Sửa" Command="{Binding EditCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Xóa" Command="{Binding DeleteCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Làm mới" Command="{Binding RefreshCommand}" Width="80"/>
        </StackPanel>
        <DataGrid ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProductID}" Width="Auto"/>
                <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Mã chủng loại" Binding="{Binding CategoryID}" Width="*"/>
                <DataGridTextColumn Header="Đơn giá" Binding="{Binding UnitPrice}" Width="*"/>
                <DataGridTextColumn Header="Đơn vị" Binding="{Binding UnitsInStock}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
